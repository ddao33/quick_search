import 'package:flutter/material.dart';

class NgramSearchWidget extends StatelessWidget {
  const NgramSearchWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
